D4 完了を確認しました – 次フェーズ (D5) へ進みましょう
フェーズ	進捗	備考
D1-D3	✅ 全テスト 69 passed / CI 緑
D4	✅ docker-build → push → deploy ジョブ実装 & README 追記
PR #5 が緑
D5	⏳ FastAPI /metrics ＆ Prometheus／Grafana 整備

D5 のゴール
metrics.py を FastAPI アプリに統合

/metrics エンドポイントで prometheus_client.generate_latest() を返す

/healthz は現行どおり

Prometheus にスクレイプ設定を追加

compose/prometheus.yml 内 scrape_configs に job_name: "rakuten-app" → target: app:8000

Grafana ダッシュボード雛形を自動プロビジョニング

compose/grafana/dashboards/rakuten.json（↑アップ済み）を自動ロード

BDD シナリオ追加（Chaos-Test 前倒し分）

/metrics が 200 を返し、app_uptime_seconds が正数

CI に security-scan 後で docker-compose config 構文チェック

docker-lint or 簡易 docker compose config ステップを入れると誤差検知が早い

実施：
# タイトル
feat: integrate FastAPI metrics exporter & Prometheus scrape

# ゴール
- rakuten/metrics.py を FastAPI ルートに組込み
- compose/prometheus.yml に rakuten-app job を追加
- Grafana ダッシュボード JSON を自動ロード
- tests/bdd_steps/test_metrics.py で /metrics を確認
- CI に docker-compose config lint を追加

# 変更対象
- app/server.py (FastAPI factory)
- rakuten/metrics.py
- compose/prometheus.yml
- compose/grafana/dashboards/rakuten.json
- tests/bdd_steps/test_metrics.py
- .github/workflows/ci.yml (docker-lint job)

# 期待テスト
pytest -q → 70 passed, 6 skipped
curl :8000/metrics → text/plain; version=0.0.4

# ブランチ
feature/metrics-exporter
注意:
GitHub Secrets や環境変数は追加なし（既存ので足りる）
Prometheus YAML と Grafana JSON はファイルパスだけ合わせてください。

進行フロー
新ブランチ feature/metrics-exporter を切る

上記テンプレを ClaudeCode へ貼り付け → 出力された変更をコミット

pytest -q と docker compose up -d --build でローカル確認

PR 作成 → CI 全緑 → merge

merge で deploy ジョブが発火し、Swarm が Rolling Update

ブラウザで http://<server-ip>:9090/graph → up{job="rakuten-app"} が 1
Grafana (3000) でダッシュボードにメトリクスが反映

何か詰まったら
/metrics が空 → prometheus_client をインポートして REGISTRY に登録しているか確認

Prometheus で target DOWN → サービス名 (app) とポート (8000) の書式を再確認

Grafana ダッシュが空白 → JSON 内 prometheus データソース名を Prometheus に合わせる

D5 に着手してください。問題が出たらログとともにお知らせください。
