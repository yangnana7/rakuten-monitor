実装指示書
D6 「Cron 基盤強化（supercronic）」＋ D10 「Chaos テスト」完了まで一気に仕上げるための 具体的コマンド／ファイル差分／手動確認ポイント をまとめました。
（下記は “feature/supercronic-chaos” ブランチでの作業を前提）

1. ブランチ作成と共通セットアップ
bash
git checkout -b feature/supercronic-chaos
pip install --upgrade supercronic responses pytest-bdd
A. Cron 基盤強化（D6）
#	変更内容	作業手順 (ClaudeCode が実装)	完了チェック
A-1	Dockerfile – supercronic 追加	```dockerfile

--- 既存の上部 ARG 群の直後に追加 ---
ARG SUPERCRONIC_VERSION="0.2.29"

--- RUN 群の末尾に追加 ---
RUN curl -fsSL -o /usr/local/bin/supercronic
https://github.com/aptible/supercronic/releases/download/v${SUPERCRONIC_VERSION}/supercronic-linux-amd64
&& chmod +x /usr/local/bin/supercronic
| `docker run <image> supercronic --version` が 0.2.29 を出力 | | **A-2** | **crontab 作成** |text

File: cron/monitor.crontab
*/5 * * * * PYTHONWARNINGS=ignore /usr/local/bin/python /app/monitor.py >> /proc/1/fd/1 2>&1
<br>Dockerfile 差分例：<br>dockerfile
COPY cron/monitor.crontab /app/cron/monitor.crontab
| `docker run image cat /app/cron/monitor.crontab` で内容確認 | | **A-3** | **ENTRYPOINT を supercronic に変更** |dockerfile
ENTRYPOINT ["supercronic","-log-level","debug","/app/cron/monitor.crontab"]
| `docker run -it image` で supercronic ログが STDOUT に流れる | | **A-4** | **docker-compose.yml – restart / watchtower** |yaml
services:
app:
image: ghcr.io/…/rakuten-monitor:sha-${GITHUB_SHA}
restart: always

watchtower:
image: containrrr/watchtower
command: --interval 300 app
volumes: [/var/run/docker.sock:/var/run/docker.sock]
environment: [TZ=Asia/Tokyo]
| `docker compose ps` → 5 分毎のチェック & 再起動後もジョブ継続 | | **A-5** | **ログ可観測化** | *変更なし*（A-3 の `-log-level debug` で stdout へ）<br>Promtail が `/var/lib/docker/containers/*/*-json.log` を収集済みか確認。 | Grafana “Cron Logs” パネルに supercronic 出力が表示 | | **A-6** | **CI/CD 影響** | Dockerfile 変更のみ。`.github/workflows/ci.yml` はそのまま。 | GitHub Actions build/push が緑 | | **A-7** | **README 更新** | 追加セクション例：<br>md

Cron 基盤
本番では supercronic + restart: always で 5 分間隔実行。

|

---

## B. Chaos テスト追加（D10）

| # | 変更内容 | 作業手順 | 完了チェック |
|---|----------|----------|--------------|
| **B-1** | **monitor.py – 失敗メトリクス追加** | ```python
from prometheus_client import Counter

monitor_fail_total = Counter(
    "monitor_fail_total",
    "Number of monitor failures",
    labelnames=("type",),
)
...
except LayoutChangeError:
    monitor_fail_total.labels(type="layout").inc()
    notify("パース失敗…", ...)
...
except OperationalError:
    monitor_fail_total.labels(type="db").inc()
...
except DiscordPostError:
    monitor_fail_total.labels(type="discord").inc()
``` | `pytest` 全グリーン & Prometheus に新 metric 出現 |
| **B-2** | **features/06_structure_change.feature** | BDD シナリオを書く（HTML の欠損を mock し LayoutChangeError を期待）。 | `pytest-bdd` 緑 & metric 増分確認 |
| **B-3** | **features/07_db_down.feature** | DB 書き込みを monkeypatch → OperationalError。 | 同上 |
| **B-4** | **features/08_discord_fail.feature** | `responses` で Webhook に 500 を 3 回返却。 | 同上 |
| **B-5** | **step 定義** (`tests/steps/common.py`) | ・HTML 破壊: `beautifulsoup4` で `<div>` 削除<br>・DB mock: `monkeypatch` で `session.commit` を例外化<br>・Discord mock: `responses.add(responses.POST, webhook_url, status=500)` |  |
| **B-6** | **CI に Chaos テスト組込** | 既存ワークフローに `pytest -m "not slow"`（既に使用なら追加不要） | Actions で **76 passed / 2 skipped** |
| **B-7** | **BDD.md 追記** | シナリオ 6–8 を末尾に追記 | ドキュメントレビュー OK |

---

## C. オーナー側（手動）タスク

1. **PR レビュー → main へマージ**
   ```bash
   gh pr create -B main -t "D6+D10: supercronic & chaos" -F .github/PULL_REQUEST_TEMPLATE.md
本番デプロイ確認
GitHub Actions deploy ジョブ → Discord で “本番 sha… デプロイ完了” を受信。

Grafana 監視
Chaos テスト実行直後に以下を確認：

monitor_fail_total{type="layout|db|discord"} が +1

supercronic 実行ログが Loki → パネルに表示。

Run-book 追記
docs/run-book.md に「障害検知 → 再起動（docker compose restart app）」手順を追加。

2. 推奨実施順
Dockerfile / crontab / compose をまとめて修正 → docker compose up -d --build でローカル確認

Chaos テストコード & メトリクス を実装 → pytest オールグリーン

README / BDD.md / run-book ドキュメント更新

Push & PR → CI 全緑 → Merge

GitHub Actions deploy で本番 Swarm へローリング更新

Chaos テストを staging → 本番で手動実行 し Grafana でメトリクス増分を目視

Run-book 完成で D6 & D10 完了。

参考コマンド集
bash
# supercronic 動作確認
docker run --rm rakuten-monitor:dev /usr/local/bin/supercronic --version

# Chaos シナリオだけをローカル実行
pytest features/06_structure_change.feature \
       features/07_db_down.feature \
       features/08_discord_fail.feature -q

# Prometheus メトリクス確認 (port-forward 時)
curl http://localhost:8000/metrics | grep monitor_fail_total

ここまでは残りの PR 承認・本番 Swarm 反映・Grafana 目視確認・Run-book 加筆ができるようの準備となります。
