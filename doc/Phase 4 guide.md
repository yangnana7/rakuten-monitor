| ステップ | 目的              | 作業概要                                                                                            | 目安工数   |
| ---- | --------------- | ----------------------------------------------------------------------------------------------- | ------ |
| 4-1  | **CI パイプライン構築** | GitHub Actions で `ruff → pytest → docker build` ワークフローを作成。成功時は `ghcr.io` にイメージ push。            | 0.5 d  |
| 4-2  | **自動デプロイ**      | 本番サーバーに `watchtower` を導入し、`latest` イメージ更新時に自動再作成。環境変数は `.env.production` に集約。                   | 0.5 d  |
| 4-3  | **集中ログ基盤**      | Loki + Promtail もしくは Vector を追加し、アプリ / Nginx / Docker ログを収集・Grafana に統合。                        | 1 d    |
| 4-4  | **アラート整備**      | Grafana Alerting で：<br>• 監視ジョブ失敗連続 3 回<br>• Discord Webhook 送信失敗<br>を通知（Discord `#ops-alerts`）。 | 0.5 d  |
| 4-5  | **リリース運用ルール**   | `main` ブランチマージ ➜ `v*.*.*` タグ ➜ CI がイメージ配布 ➜ 本番自動更新、を README に明文化。                               | 0.25 d |
