現状整理
事項	状態
.feature 4 本・ステップ定義・フィクスチャ	作成済み
pytest -m bdd	RED（統合ポイントで失敗）
DoD（全テスト緑＋CI緑）	未達

結論：次へ進む前に D3 を緑にしてから - そうしないと D4 の CI／デプロイ・ジョブが必ず落ちます。

優先順位とやるべきこと
優先	タスク	具体的アクション
★★★★☆	BDD を緑にする	1) 失敗シナリオを 1 つずつ pytest -m bdd -vv で確認
2) 下記の「統合ポイント調整ガイド」に従い、monitor.py / ステップ定義を修正
★★★☆☆	ユニット⇆BDD の重複整理	1) 既存ユニットのヘルパを conftest.py へ寄せる
2) 共通モック（Discord、DB）を fixture 化
★★☆☆☆	CI に bdd ジョブを復活	テスト緑を確認後、 .github/workflows/ci.yml の bdd job を needs: unit で有効化

統合ポイント調整ガイド（よくハマる 3 か所）
ポイント	症状	修正ヒント
1. Discord モック	AttributeError: send など	ステップ定義で
monkeypatch.setattr("rakuten.discord_client.DiscordClient.send", dummy.append)
2. DB パス差し替え	SQLite ファイルが作られずクエリ失敗	monkeypatch.setenv("DB_PATH", str(tmp_path / "db.sqlite"))
＋ ItemDB に check_same_thread=False を渡すと安全
3. HTML フィクスチャ URL	Parser が 404 / 空リストで比較失敗	pytest_httpserver で /list を返し、 LIST_URL をその URL に設定

デバッグの進め方（例：restock が失敗）
bash


pytest tests/bdd_steps/test_restock.py::test_detect_restock -vv
どのステップで落ちたか を見る

スタックトレース末尾 が Parser なら → HTML / パーサを確認
Discord なら → モックが効いているか確認

必要なら print(dummy_messages) を差し込んで実際の送信内容を確認

最小修正パッチ例
python


# common_steps.py 末尾
@then(parsers.parse('no Discord message is sent'))
def assert_no_message(dummy_messages):
    assert not dummy_messages, f"unexpected messages: {dummy_messages}"
diff


# monitor.py inside main()
- if not new_items and not restocked_items:
-     return
+ if not (new_items or restocked_items):
+     logger.info("No change – exiting")
+     return
ゴールチェック
bash


export DISCORD_WEBHOOK_URL=dummy START_TIME=08:00 END_TIME=20:00
pytest -q            # 62 unit + 7 (time + 4 new) = 69 passed, 6 skipped
進め方提案
今日中にシナリオ 4 本をすべて緑に。

その PR が CI 緑になったら D3 完了 とし、 D4（CI deploy 拡張）へ。

調整でつまずいたらエラーログを貼ってください。重点箇所をピンポイントで修正します。
