ステップD1～D2

着手前に下記を軽くチェックしてください。

ローカルブランチ状態

main を最新に pull 済みか

作業用に feature/bdd-init などを切る

.env（または GitHub Secrets）に最低限のキー

DISCORD_WEBHOOK_URL ― 空でも可（BDD ではダミーを注入）

START_TIME / END_TIME ― 未設定ならデフォルトは 08:00 / 20:00

pytest 既存 45 本が緑

失敗がある場合は先に原因を潰してから D1-D2 へ

ここまで OK なら D1（.feature 化＋マーカー追加）→ D2（監視時間帯ロジックとステップ定義／CI 反映） を順に実装して大丈夫です。
疑問点が出たら都度聞いてください。

D1〜D2
1. BDD.md → .feature 化（D1）
修正方針	具体例
設定ファイル表記を .env / 環境変数に置き換え	Given the environment variable "LIST_URL" is set to "...", "START_TIME" to "08:00", "END_TIME" to "20:00"
database.json はそのまま（現コードは sqlite だが後方互換のため温存。後日 Timescale へ移行ステップで統一）

新規ディレクトリ features/ に５本のシナリオを分割し、すべて @bdd マーカーを付与。

2. ステップ定義スケルトン調整（D1）
python
# tests/bdd_steps/test_time_window.py
import os, pytest, datetime as dt
from freezegun import freeze_time
from rakuten.monitor import main as run_monitor

@pytest.mark.bdd
@freeze_time("2025-07-31 09:00:00+09:00")
def test_within_time_window(monkeypatch, tmp_path, capsys):
    monkeypatch.setenv("START_TIME", "08:00")
    monkeypatch.setenv("END_TIME", "20:00")
    monkeypatch.setenv("DISCORD_WEBHOOK_URL", "dummy")  # ダミー通知
    run_monitor()
    assert "Checking" in capsys.readouterr().out
3. コード最小パッチ（D2）
diff
# monitor.py 先頭付近
+import os, datetime as dt

+def _within_watch_window() -> bool:
+    start = os.getenv("START_TIME", "00:00")
+    end   = os.getenv("END_TIME",   "23:59")
+    now   = dt.datetime.now().time()
+    st, et = (dt.time.fromisoformat(start), dt.time.fromisoformat(end))
+    return st <= now <= et

 def main() -> int:
-    WEBHOOK_URL = settings.get_webhook_url()
+    if not _within_watch_window():
+        logger.info("outside watch window – exiting")
+        return 0
+    WEBHOOK_URL = settings.get_webhook_url()
     ...
既存 API は保持しつつ、環境変数だけで完結。Docker 側にも
START_TIME, END_TIME を追記（デフォルトは 08-20 時）

4. CI ワークフロー差分
yaml
  bdd:
    needs: unit
    env:
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL || 'dummy' }}
      START_TIME: "08:00"
      END_TIME:   "20:00"
    steps:
      - uses: actions/checkout@v4
      - run: pip install -r requirements.txt
      - run: pytest -m bdd -q
次の一手
BDD.md を修正し、.env 方式に置換した .feature を commit（今日中）

最小パッチを monitor.py に適用し、既存 45 テスト＋BDD が緑になることを確認

docker-compose.yml に START_TIME/END_TIME を追加（値は 08:00〜20:00）

README に「監視時間帯は環境変数で変更可」と明記

これで D1〜D2 完了の定義が現行成果物にフィットします。
